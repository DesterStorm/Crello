import React from 'react'

const CrelloList = ({title}) => {
  return (
    <div style>
    <h4>{title}</h4>
    </div>
  )
}

const styles = {
  container: {
    backgroundColor: '#ccc',
    borderRadius: 3
  }
}

export default CrelloList 