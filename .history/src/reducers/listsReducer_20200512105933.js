
const initialState = [
  {
    title: 'this is a list title'
  }
]

const listsReducer = (state, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default listsReducer